{{#each customselectors}}{{this}},{{/each}}
{{prefix}}{{name}} {
  background-image: url('{{datauri}}');
  @include icon;
  
  &.autosize {
    height: {{height}};
    width: {{width}};
  }
  
  &.intrinsic {
    padding-top: {{height}} / {{width}} * 100%;
  }
}